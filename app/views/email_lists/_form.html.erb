<%= simple_form_for(@email_list) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email_id %>
    <%= f.input :survey_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
